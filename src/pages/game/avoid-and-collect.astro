---
import Layout from '~/layouts/layout.astro';
import { GameContent } from '~/components/common/game-content/';
import { Content } from '~/components/avoid-and-collect/content';

export const title = 'Avoid & Collect';
export const publish = import.meta.env.DEV || false;
---

<Layout title={title}>
  <GameContent>
    <Content client:only />
  </GameContent>
</Layout>
